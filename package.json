{
  "name": "saas-kubit",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:verbose": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "sharp": "^0.34.5",
    "vitest": "^3.0.0"
  }
}
